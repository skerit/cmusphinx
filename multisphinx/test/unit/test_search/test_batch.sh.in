#!/bin/sh

DATADIR=@top_srcdir@/test/data
MODELDIR=@top_srcdir@/model
PROGDIR=@top_builddir@/src/programs

$PROGDIR/pocketsphinx_batch \
    -ctl "$DATADIR/chan3_2.ctl" \
    -cepdir "$DATADIR" -cepext .raw \
    -adcin yes -lm "$DATADIR/hub4.5000.DMP" \
    -dict "$DATADIR/hub4.5000.dic" \
    -hmm "$DATADIR/hub4wsj_sc_8k" \
    -backtrace yes \
    -hyp test_batch.hyp

